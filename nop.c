#include "monty.h"

/**
 * nop_func - Does nothing.
 * @head: Double pointer to the head of the stack.
 * @line_number: The line number where the function is called.
 * Description: This function is a no-operation (nop) function that
 * does nothing. It is used as a placeholder or for testing purposes, and it
 * does not modify the stack or perform any operations.
 * Return: Nothing
 */

void nop_func(stack_t **head, unsigned int line_number)
{
	(void) head;
	(void) line_number;
}
